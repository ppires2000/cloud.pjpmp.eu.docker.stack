---
- name: "Create wetty folder structure"
  hosts: localhost
  become: True
  become_method: sudo
  tasks:
    - name: "Create wetty folder structure"
      file:
        path: "{{ apps_folder }}/wetty"
        state: directory
        owner: root
        group: root